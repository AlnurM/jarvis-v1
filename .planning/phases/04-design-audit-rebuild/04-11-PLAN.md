---
phase: 04-design-audit-rebuild
plan: 11
type: execute
wave: 2
depends_on: ["04-06"]
files_modified:
  - frontend/src/modes/ThinkingMode.tsx
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "ThinkingMode shows 'JARVIS PROCESSING QUERY...' text top-left in Space Grotesk"
    - "ThinkingMode shows small floating particles around the orb"
    - "ThinkingMode shows subtle status text bottom-right"
    - "ThinkingMode remains full-screen (no AppShell)"
  artifacts:
    - path: "frontend/src/modes/ThinkingMode.tsx"
      provides: "Stitch-fidelity thinking mode with status text and particles"
      contains: "PROCESSING QUERY"
  key_links:
    - from: "frontend/src/modes/ThinkingMode.tsx"
      to: "frontend/src/components/OrbAnimation.tsx"
      via: "import OrbAnimation"
      pattern: "OrbAnimation"
---

<objective>
Add missing elements to ThinkingMode: "JARVIS PROCESSING QUERY..." status text top-left, small floating particles around the orb, and subtle status text bottom-right. ThinkingMode remains full-screen (no AppShell).

Purpose: Close the ThinkingMode gap. Current implementation has the correct orb but is missing the status text and floating particles shown in the Stitch screen. Per CLAUDE.md, Stitch is source of truth — it shows text, so we add text.

Output: Updated ThinkingMode.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-design-audit-rebuild/04-CONTEXT.md
@.planning/phases/04-design-audit-rebuild/STITCH-SPECS.md
@frontend/src/modes/ThinkingMode.tsx
@frontend/src/components/OrbAnimation.tsx
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add status text and floating particles to ThinkingMode</name>
  <files>frontend/src/modes/ThinkingMode.tsx</files>
  <read_first>
    - frontend/src/modes/ThinkingMode.tsx (current implementation)
    - frontend/src/components/OrbAnimation.tsx
    - .planning/phases/04-design-audit-rebuild/STITCH-SPECS.md
  </read_first>
  <action>
Update ThinkingMode.tsx to add the missing Stitch elements. Keep the existing orb and ambient glow. Add three things:

**1. Top-left status text — "JARVIS PROCESSING QUERY...":**

Add this as an absolutely positioned element inside the existing root div, ABOVE the orb:
```jsx
<p
  className="absolute top-8 left-8"
  style={{
    fontFamily: 'var(--font-label)',
    fontSize: '0.625rem',
    color: 'var(--color-on-surface-variant)',
    letterSpacing: '0.15em',
    textTransform: 'uppercase',
    opacity: 0.5,
  }}
>
  JARVIS PROCESSING QUERY...
</p>
```

**2. Floating particles around the orb (6-8 small dots):**

Create an array of particle configs and render them as animated divs. Place this AFTER the orb motion.div:
```jsx
{/* Floating particles around orb */}
{Array.from({ length: 8 }, (_, i) => {
  const angle = (i / 8) * Math.PI * 2
  const radius = 180 + (i % 3) * 30  // vary distance
  const x = Math.cos(angle) * radius
  const y = Math.sin(angle) * radius
  const size = 2 + (i % 3)  // 2-4px
  const duration = 3 + (i % 4) * 0.5  // 3-5s
  const delay = i * 0.3

  return (
    <motion.div
      key={i}
      className="absolute rounded-full"
      style={{
        width: size,
        height: size,
        background: i % 2 === 0 ? '#85adff' : '#ad89ff',
        left: `calc(50% + ${x}px)`,
        top: `calc(50% + ${y}px)`,
        transform: 'translate(-50%, -50%)',
      }}
      animate={{
        opacity: [0.1, 0.5, 0.1],
        scale: [0.8, 1.2, 0.8],
        x: [0, (i % 2 === 0 ? 10 : -10), 0],
        y: [0, (i % 2 === 0 ? -8 : 8), 0],
      }}
      transition={{
        duration: duration,
        repeat: Infinity,
        delay: delay,
        ease: [0.22, 1, 0.36, 1],
      }}
    />
  )
})}
```

**3. Bottom-right status text:**
```jsx
<p
  className="absolute bottom-8 right-8"
  style={{
    fontFamily: 'var(--font-label)',
    fontSize: '0.5rem',
    color: 'var(--color-on-surface-variant)',
    letterSpacing: '0.1em',
    textTransform: 'uppercase',
    opacity: 0.3,
  }}
>
  NEURAL PROCESSING ACTIVE
</p>
```

**Keep everything else unchanged:**
- Root div stays `w-screen h-screen` (ThinkingMode is NOT in AppShell — it's full-screen)
- Outer ambient glow motion.div stays
- Core orb with hue-rotate animation stays
- Background stays `var(--color-background)`

**Design rules:**
- All text uses on-surface-variant, NOT pure white (D-10)
- Particles use primary (#85adff) and secondary (#ad89ff) colors
- Easing: [0.22, 1, 0.36, 1] (D-07)
  </action>
  <verify>
    <automated>cd /Users/alikeforalike/Documents/Dev/jarvis-v1 && grep -c "PROCESSING QUERY" frontend/src/modes/ThinkingMode.tsx && grep -c "NEURAL PROCESSING" frontend/src/modes/ThinkingMode.tsx && grep -c "rounded-full" frontend/src/modes/ThinkingMode.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Contains "JARVIS PROCESSING QUERY..." text positioned top-left (absolute top-8 left-8)
    - Contains "NEURAL PROCESSING ACTIVE" text positioned bottom-right (absolute bottom-8 right-8)
    - Contains 8 floating particle divs with alternating #85adff and #ad89ff colors
    - Particles animate opacity, scale, x, y with custom easing
    - Root div still uses `w-screen h-screen` (NOT wrapped in AppShell)
    - OrbAnimation still present with scale={1.2}
    - No pure white #FFFFFF text
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>ThinkingMode updated with "JARVIS PROCESSING QUERY..." top-left, 8 floating particles around orb in primary/secondary colors, "NEURAL PROCESSING ACTIVE" bottom-right. Full-screen layout preserved. Build passes.</done>
</task>

</tasks>

<verification>
- ThinkingMode shows status text top-left and bottom-right
- 8 floating particles visible around orb
- Full-screen layout preserved (no AppShell)
- `npm run build` succeeds
</verification>

<success_criteria>
ThinkingMode matches Stitch screen: processing status text top-left, floating particles around orb, status text bottom-right. Full-screen immersive layout preserved without AppShell wrapper.
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-audit-rebuild/04-11-SUMMARY.md`
</output>
