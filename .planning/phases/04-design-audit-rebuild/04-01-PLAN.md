---
phase: 04-design-audit-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/index.html
autonomous: true
requirements: []

must_haves:
  truths:
    - "@theme block contains all design tokens from Stitch Design System screen"
    - "Token values match Stitch screen exactly — not guessed from design.md text"
    - "Space Grotesk weight 700 available for temperature/prayer displays"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Complete @theme token layer"
      contains: "--color-primary-dim"
    - path: "frontend/index.html"
      provides: "Google Fonts with correct weights"
      contains: "Space+Grotesk:wght@400;500;600;700"
  key_links:
    - from: "frontend/src/index.css"
      to: "all mode components"
      via: "CSS custom properties consumed by Tailwind utilities and var() references"
      pattern: "--color-.*:"
---

<objective>
Pull the Stitch Design System screen and expand the @theme token layer in index.css to include ALL required design tokens. Fix Space Grotesk font weight in index.html.

Purpose: Every subsequent mode rebuild plan depends on having correct, complete design tokens. Tokens are the foundation — wrong tokens mean wrong visuals everywhere.
Output: Complete @theme block with all tokens verified against Stitch, corrected font loading.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-audit-rebuild/04-CONTEXT.md
@.planning/phases/04-design-audit-rebuild/04-RESEARCH.md
@design.md

<interfaces>
<!-- Current @theme tokens in frontend/src/index.css (7 tokens + 3 non-color): -->
--color-background: #0e0e0e;
--color-surface-low: #131313;
--color-surface-high: #201f1f;
--color-primary: #85adff;
--color-secondary: #ad89ff;
--color-on-surface: #e6e1e5;
--color-on-surface-variant: #adaaaa;
--font-display: Inter, sans-serif;
--font-label: "Space Grotesk", sans-serif;
--ease-glass: cubic-bezier(0.22, 1, 0.36, 1);

<!-- Missing tokens per D-03 and RESEARCH.md: -->
--color-primary-dim         (ambient glow shadows, D-12)
--color-secondary-dim       (ambient glow shadows, D-12)
--color-surface-container-lowest  (recessed "carved" look)
--color-surface-container-highest (protruding "lifted" elements)
--color-primary-container   (AI Pulse inner layer, D-08)
--color-secondary-container (AI Pulse outer layer, D-08)
--color-outline-variant     (ghost border at 15% opacity)
--color-primary-fixed       (tertiary button text)
--radius-xl                 (signature corner radius, 1.5rem)

<!-- Token rename needed: -->
--color-surface-low  -> --color-surface-container-low
--color-surface-high -> --color-surface-container-high
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pull Stitch Design System screen and extract token values</name>
  <files>none (extraction only)</files>
  <read_first>
    - design.md (token reference table in section 7)
    - frontend/src/index.css (current @theme block)
  </read_first>
  <action>
    1. Load the figma-implement-design skill by invoking the Skill tool with `figma:figma-implement-design`
    2. Call `mcp__figma__get_design_context` with fileKey="7359010342585899885" and nodeId="asset-stub-assets-12c7eab3ced040a3bf6965e467c9a5cf-1775654289866"
    3. Call `mcp__figma__get_screenshot` with the same fileKey and nodeId to visually confirm token values
    4. From the Stitch output, extract exact hex values for ALL tokens listed in design.md section 7, plus:
       - primary-dim (the muted/dimmed variant of primary #85adff)
       - secondary-dim (the muted/dimmed variant of secondary #ad89ff)
       - surface-container-lowest (darker than surface-container-low)
       - surface-container-highest (lighter than surface-container-high)
       - primary-container (lighter/muted primary for AI Pulse)
       - secondary-container (lighter/muted secondary for AI Pulse)
       - outline-variant (ghost border color)
       - primary-fixed (tertiary button text color)
    5. Per D-04: If Stitch values conflict with design.md text, Stitch wins.
    6. Record all extracted values — they will be used in Task 2.
  </action>
  <verify>
    Stitch Design System screen data has been retrieved via MCP. Token hex values have been extracted and are ready for implementation.
  </verify>
  <acceptance_criteria>
    - mcp__figma__get_design_context was called with fileKey="7359010342585899885" and nodeId containing "asset-stub-assets"
    - Token values extracted include at minimum: background, primary, secondary, surface-container-low, surface-container-high, primary-container, secondary-container
  </acceptance_criteria>
  <done>All design token values extracted from Stitch Design System screen, ready for CSS implementation</done>
</task>

<task type="auto">
  <name>Task 2: Expand @theme tokens and fix font loading</name>
  <files>frontend/src/index.css, frontend/index.html</files>
  <read_first>
    - frontend/src/index.css (current state — will be modified)
    - frontend/index.html (current Google Fonts link)
    - design.md (section 7 token reference for naming)
  </read_first>
  <action>
    Using the exact values extracted from Stitch in Task 1, update frontend/src/index.css:

    1. RENAME existing tokens to match design.md naming convention:
       - `--color-surface-low` -> `--color-surface-container-low`
       - `--color-surface-high` -> `--color-surface-container-high`

    2. ADD all missing tokens with values from Stitch (use Stitch values, not these fallbacks):
       - `--color-primary-dim: <from-stitch>;` (fallback if Stitch unclear: #85adff — use at low opacity in shadows)
       - `--color-secondary-dim: <from-stitch>;` (fallback: #ad89ff)
       - `--color-surface-container-lowest: <from-stitch>;` (fallback: #0b0b0b)
       - `--color-surface-container-highest: <from-stitch>;` (fallback: #2a2929)
       - `--color-primary-container: <from-stitch>;` (for AI Pulse inner layer)
       - `--color-secondary-container: <from-stitch>;` (for AI Pulse outer layer)
       - `--color-outline-variant: <from-stitch>;` (ghost border color)
       - `--color-primary-fixed: <from-stitch>;` (tertiary text)
       - `--radius-xl: 1.5rem;` (confirmed in design.md)

    3. SEARCH for any usage of old token names `surface-low` or `surface-high` in frontend/src/:
       - grep for `surface-low` and `surface-high` in all .tsx/.ts/.css files
       - Update any references found to use the new names `surface-container-low` / `surface-container-high`

    4. UPDATE frontend/index.html Google Fonts link:
       - Change Space Grotesk weights from `wght@400;500;600` to `wght@400;500;600;700`
       - Weight 700 is needed for large temperature and prayer name displays

    Preserve all existing CSS rules below the @theme block (html/body/#root styles, landscape media query).
  </action>
  <verify>
    <automated>cd /Users/alikeforalike/Documents/Dev/jarvis-v1/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/index.css contains `--color-primary-dim:`
    - frontend/src/index.css contains `--color-secondary-dim:`
    - frontend/src/index.css contains `--color-surface-container-lowest:`
    - frontend/src/index.css contains `--color-surface-container-highest:`
    - frontend/src/index.css contains `--color-primary-container:`
    - frontend/src/index.css contains `--color-secondary-container:`
    - frontend/src/index.css contains `--color-outline-variant:`
    - frontend/src/index.css contains `--color-primary-fixed:`
    - frontend/src/index.css contains `--radius-xl: 1.5rem`
    - frontend/src/index.css contains `--color-surface-container-low:` (renamed from surface-low)
    - frontend/src/index.css contains `--color-surface-container-high:` (renamed from surface-high)
    - frontend/src/index.css does NOT contain `--color-surface-low:` (old name removed)
    - frontend/src/index.css does NOT contain `--color-surface-high:` (old name removed, note: surface-container-high is fine)
    - frontend/index.html contains `Space+Grotesk:wght@400;500;600;700`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>@theme block contains all required tokens with Stitch-verified values, old token names renamed, Space Grotesk 700 loaded, build passes</done>
</task>

</tasks>

<verification>
- `npm run build` passes (TypeScript + Vite compile with new token names)
- `grep -c "color-primary-dim" frontend/src/index.css` returns 1
- `grep -c "radius-xl" frontend/src/index.css` returns 1
- `grep "surface-low:" frontend/src/index.css` returns 0 matches (old name gone)
- `grep "wght@400;500;600;700" frontend/index.html` returns 1 match
</verification>

<success_criteria>
All design tokens from Stitch Design System screen are present in @theme with verified values. Token naming matches design.md convention. Space Grotesk 700 weight loads. Build passes clean.
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-audit-rebuild/04-01-SUMMARY.md`
</output>
