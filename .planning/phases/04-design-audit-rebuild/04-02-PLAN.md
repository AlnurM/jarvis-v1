---
phase: 04-design-audit-rebuild
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/components/OrbAnimation.tsx
  - frontend/src/modes/ThinkingMode.tsx
  - frontend/src/modes/ListeningMode.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "OrbAnimation uses primary-container and secondary-container tokens from @theme, not raw primary/secondary"
    - "All animation easing in OrbAnimation and ThinkingMode uses cubic-bezier array [0.22, 1, 0.36, 1], not string 'easeInOut'"
    - "ListeningMode and ThinkingMode backgrounds use var(--color-background), not hardcoded #0a0a0a"
    - "ThinkingMode and ListeningMode visually match their respective Stitch screens"
  artifacts:
    - path: "frontend/src/components/OrbAnimation.tsx"
      provides: "AI Pulse orb with correct tokens and easing"
      contains: "primary-container"
    - path: "frontend/src/modes/ThinkingMode.tsx"
      provides: "Thinking mode matching Stitch screen"
      contains: "var(--color-background)"
    - path: "frontend/src/modes/ListeningMode.tsx"
      provides: "Listening mode matching Stitch screen"
      contains: "var(--color-background)"
  key_links:
    - from: "frontend/src/components/OrbAnimation.tsx"
      to: "frontend/src/index.css"
      via: "CSS custom property var() references"
      pattern: "var\\(--color-primary-container\\)"
    - from: "frontend/src/modes/ThinkingMode.tsx"
      to: "frontend/src/components/OrbAnimation.tsx"
      via: "React component import"
      pattern: "import.*OrbAnimation"
---

<objective>
Pull Stitch screens for Thinking Mode and Listening Mode, then rebuild OrbAnimation, ThinkingMode, and ListeningMode to match Stitch pixel-for-pixel.

Purpose: OrbAnimation (AI Pulse) is the signature element used by ThinkingMode and the idle state. It must use the correct container tokens and glass easing. ThinkingMode and ListeningMode are the first modes users see during the voice loop.
Output: Three components rebuilt to Stitch fidelity with correct tokens, easing, and colors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-audit-rebuild/04-CONTEXT.md
@.planning/phases/04-design-audit-rebuild/04-RESEARCH.md
@.planning/phases/04-design-audit-rebuild/04-01-SUMMARY.md
@design.md

<interfaces>
<!-- OrbAnimation current props interface: -->
interface OrbAnimationProps {
  primaryColor?: string   // default: 'var(--color-primary)'
  secondaryColor?: string // default: 'var(--color-secondary)'
  scale?: number          // default: 1
}

<!-- Tokens available after Plan 01 (from @theme): -->
--color-primary-container   (AI Pulse inner layer)
--color-secondary-container (AI Pulse outer layer)
--color-primary-dim         (ambient shadows)
--color-secondary-dim       (ambient shadows)
--color-background          (#0e0e0e — replaces hardcoded #0a0a0a)
--ease-glass                (cubic-bezier(0.22, 1, 0.36, 1))

<!-- ThinkingMode uses: -->
- OrbAnimation component with scale={1.2}
- Outer ambient glow div with radial gradient
- Inner hue-rotate animation wrapper
- Hardcoded background '#0a0a0a' (should be var(--color-background))
- Inner animation uses ease: 'easeInOut' (should be [0.22, 1, 0.36, 1])

<!-- ListeningMode uses: -->
- Canvas waveform via useWaveVisualizer hook
- WAVE_COLOR = '#00d4ff' (verify against Stitch)
- Hardcoded background '#0a0a0a' (should be var(--color-background))
- "Listening..." label with Space Grotesk
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pull Stitch screens for Thinking Mode and Listening Mode</name>
  <files>none (extraction only)</files>
  <read_first>
    - frontend/src/components/OrbAnimation.tsx (current implementation)
    - frontend/src/modes/ThinkingMode.tsx (current implementation)
    - frontend/src/modes/ListeningMode.tsx (current implementation)
    - design.md (AI Pulse spec in section 5, Do's and Don'ts in section 6)
  </read_first>
  <action>
    1. Load the figma-implement-design skill by invoking the Skill tool with `figma:figma-implement-design`
    2. Pull Thinking Mode screen:
       - Call `mcp__figma__get_design_context` with fileKey="7359010342585899885" and nodeId="c121cc95f2e149a0873accbd6c47d7bd"
       - Call `mcp__figma__get_screenshot` with same fileKey and nodeId
    3. Pull Listening Mode screen:
       - Call `mcp__figma__get_design_context` with fileKey="7359010342585899885" and nodeId="d6bf4b24d8844d3ba4aa32d422a6a8c4"
       - Call `mcp__figma__get_screenshot` with same fileKey and nodeId
    4. Compare Stitch output against current implementation and identify ALL divergences:
       - Background color: is it #0a0a0a or #0e0e0e?
       - OrbAnimation: what colors does Stitch use for inner/outer layers? Do they match primary-container/secondary-container?
       - OrbAnimation: what blur values, opacity ranges, and animation timings does Stitch show?
       - ListeningMode: what is the exact waveform color? Is #00d4ff correct?
       - ListeningMode: label text styling — font size, opacity, spacing
       - ThinkingMode: outer glow size, blur, opacity ranges
       - Any additional visual elements present in Stitch but missing from current code
    5. Record all divergences — they drive the code changes in Tasks 2 and 3.
  </action>
  <verify>
    Both Stitch screens retrieved via MCP. Divergence list documented for OrbAnimation, ThinkingMode, and ListeningMode.
  </verify>
  <acceptance_criteria>
    - mcp__figma__get_design_context called with nodeId="c121cc95f2e149a0873accbd6c47d7bd" (Thinking)
    - mcp__figma__get_design_context called with nodeId="d6bf4b24d8844d3ba4aa32d422a6a8c4" (Listening)
    - Divergence list identifies at minimum: background color, orb colors, easing values
  </acceptance_criteria>
  <done>Stitch screens for Thinking and Listening modes retrieved, divergences documented</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild OrbAnimation and ThinkingMode to match Stitch</name>
  <files>frontend/src/components/OrbAnimation.tsx, frontend/src/modes/ThinkingMode.tsx</files>
  <read_first>
    - frontend/src/components/OrbAnimation.tsx (current state)
    - frontend/src/modes/ThinkingMode.tsx (current state)
    - frontend/src/index.css (token values after Plan 01)
  </read_first>
  <action>
    Per D-05 (targeted rebuild, not full rewrite) and D-08 (AI Pulse spec), fix only what diverges from Stitch:

    **OrbAnimation.tsx changes (per D-08, D-07):**
    1. Change default primaryColor prop from `'var(--color-primary)'` to `'var(--color-primary-container)'`
    2. Change default secondaryColor prop from `'var(--color-secondary)'` to `'var(--color-secondary-container)'`
    3. Replace ALL `ease: 'easeInOut'` with `ease: [0.22, 1, 0.36, 1]` in both motion.div transition objects
    4. Verify blur values match Stitch: inner should be blur(40px), outer should be blur(80px)
    5. Verify breathing opacity range matches Stitch: outer 0.4->1->0.4, inner 0.6->1->0.6 with scale 0.95->1.05->0.95
    6. Adjust any values that diverge from Stitch output (from Task 1)

    **ThinkingMode.tsx changes:**
    1. Replace `background: '#0a0a0a'` with `background: 'var(--color-background)'` (per Pitfall 6)
    2. Replace `ease: 'easeInOut'` on the hue-rotate animation (line ~52) with `ease: [0.22, 1, 0.36, 1]` (per D-07)
    3. Update the OrbAnimation invocation if Stitch shows different colors than what the default props now provide
    4. Update the outer ambient glow gradient color from `${THINKING_SECONDARY}40` to use `var(--color-secondary-dim)` if appropriate based on Stitch
    5. Adjust any other values that diverge from Stitch Thinking Mode screen

    **Preserve:** Component interfaces (OrbAnimationProps), import structure, overall component architecture.
  </action>
  <verify>
    <automated>cd /Users/alikeforalike/Documents/Dev/jarvis-v1/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - OrbAnimation.tsx contains `var(--color-primary-container)` as default primaryColor
    - OrbAnimation.tsx contains `var(--color-secondary-container)` as default secondaryColor
    - OrbAnimation.tsx does NOT contain the string `easeInOut`
    - OrbAnimation.tsx contains `[0.22, 1, 0.36, 1]` (at least 2 occurrences — one per motion.div)
    - ThinkingMode.tsx contains `var(--color-background)` and does NOT contain `#0a0a0a`
    - ThinkingMode.tsx does NOT contain the string `easeInOut`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>OrbAnimation uses container tokens and glass easing. ThinkingMode uses design token background and correct easing throughout. Both match Stitch screen.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild ListeningMode to match Stitch</name>
  <files>frontend/src/modes/ListeningMode.tsx</files>
  <read_first>
    - frontend/src/modes/ListeningMode.tsx (current state)
    - frontend/src/index.css (token values after Plan 01)
  </read_first>
  <action>
    Per D-05 (targeted rebuild) and Stitch divergences from Task 1:

    1. Replace `background: '#0a0a0a'` with `background: 'var(--color-background)'` (per Pitfall 6)
    2. Verify WAVE_COLOR `#00d4ff` against Stitch Listening Mode screen:
       - If Stitch shows a different blue, update WAVE_COLOR to the Stitch value
       - If Stitch matches `#00d4ff`, keep as-is
       - If Stitch shows the primary token color (#85adff), change to `var(--color-primary)` reference
    3. Check the "Listening..." label styling against Stitch:
       - Font size, letter spacing, opacity, position
       - Update if diverging
    4. Check if Stitch shows any additional visual elements (e.g., ambient glow behind waveform, different waveform sizing)
    5. Ensure the `-webkit-backdrop-filter` is paired with `backdrop-filter` if any blur is added (Pitfall 3)
    6. Adjust any other divergences found in Task 1's Stitch comparison

    **Preserve:** Canvas waveform rendering logic (useWaveVisualizer hook integration), component props interface, cleanup logic.
  </action>
  <verify>
    <automated>cd /Users/alikeforalike/Documents/Dev/jarvis-v1/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ListeningMode.tsx contains `var(--color-background)` and does NOT contain `#0a0a0a`
    - ListeningMode.tsx WAVE_COLOR value matches Stitch output (either kept as #00d4ff if confirmed, or updated)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>ListeningMode matches Stitch Listening Mode screen — correct background, waveform color, and label styling</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `grep -n "easeInOut" frontend/src/components/OrbAnimation.tsx frontend/src/modes/ThinkingMode.tsx` returns zero matches
- `grep -n "#0a0a0a" frontend/src/components/OrbAnimation.tsx frontend/src/modes/ThinkingMode.tsx frontend/src/modes/ListeningMode.tsx` returns zero matches
- `grep -c "primary-container" frontend/src/components/OrbAnimation.tsx` returns at least 1
</verification>

<success_criteria>
OrbAnimation, ThinkingMode, and ListeningMode all match their Stitch screens. Glass easing used everywhere. Design token backgrounds replace hardcoded hex. AI Pulse uses container token variants.
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-audit-rebuild/04-02-SUMMARY.md`
</output>
