---
phase: 04-design-audit-rebuild
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/modes/SpeakingMode.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "SpeakingMode background uses var(--color-background), not hardcoded #0a0a0a"
    - "SpeakingMode visually matches its Stitch screen — waveform color, subtitle styling, atmospheric glow"
    - "Glassmorphism elements (if any) include -webkit-backdrop-filter for iPad Safari"
  artifacts:
    - path: "frontend/src/modes/SpeakingMode.tsx"
      provides: "Speaking mode matching Stitch screen"
      contains: "var(--color-background)"
  key_links:
    - from: "frontend/src/modes/SpeakingMode.tsx"
      to: "frontend/src/index.css"
      via: "CSS custom property var() references"
      pattern: "var\\(--color-"
---

<objective>
Pull the Stitch Speaking Mode screen and rebuild SpeakingMode.tsx to match pixel-for-pixel.

Purpose: SpeakingMode is shown every time JARVIS responds — it is the most-seen mode in the app. Visual fidelity here directly impacts perceived quality.
Output: SpeakingMode rebuilt to Stitch fidelity with correct tokens, colors, and styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-audit-rebuild/04-CONTEXT.md
@.planning/phases/04-design-audit-rebuild/04-RESEARCH.md
@.planning/phases/04-design-audit-rebuild/04-01-SUMMARY.md
@design.md

<interfaces>
<!-- SpeakingMode current implementation details: -->
- WAVE_COLOR = '#9b59b6' (purple/violet, may differ from Stitch)
- Background: '#0a0a0a' hardcoded (should be var(--color-background))
- Atmospheric glow: radial gradient with WAVE_COLOR at 30% opacity
- Subtitle: uses var(--color-on-surface-variant) correctly (D-10 compliant)
- Subtitle transition: uses [0.22, 1, 0.36, 1] correctly
- "Tap to stop" hint: WAVE_COLOR at 40% opacity, fades out after 2s
- Canvas waveform via useWaveVisualizer hook
- Glassmorphism: NOT currently applied to any element in SpeakingMode

<!-- Props interface: -->
interface SpeakingModeProps {
  analyserRef?: React.RefObject<AnalyserNode | null>
  onTap?: () => void
}

<!-- Tokens available after Plan 01: -->
--color-background: #0e0e0e
--color-secondary: #ad89ff (may be correct waveform color per Stitch)
--color-on-surface-variant: #adaaaa
--color-primary-dim / --color-secondary-dim (for ambient shadows)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pull Stitch Speaking Mode screen and extract design specs</name>
  <files>none (extraction only)</files>
  <read_first>
    - frontend/src/modes/SpeakingMode.tsx (current implementation)
    - design.md (section 5 components, section 6 do's and don'ts)
  </read_first>
  <action>
    1. Load the figma-implement-design skill by invoking the Skill tool with `figma:figma-implement-design`
    2. Call `mcp__figma__get_design_context` with fileKey="7359010342585899885" and nodeId="8554ef1a3efa42f9a07ad8774a690a7d"
    3. Call `mcp__figma__get_screenshot` with same fileKey and nodeId
    4. Compare Stitch output against current SpeakingMode implementation and identify ALL divergences:
       - Background color: #0a0a0a vs #0e0e0e vs something else?
       - Waveform color: is it #9b59b6 or the secondary token #ad89ff or another purple?
       - Atmospheric glow: size, blur, opacity, color
       - Subtitle text: font, size, color, position, max lines, text shadow
       - "Tap to stop" hint: present in Stitch? styling?
       - Any glassmorphism cards or surfaces present in the Stitch screen?
       - Any elements in Stitch that are missing from current code
    5. Record all divergences for Task 2.
  </action>
  <verify>
    Stitch Speaking Mode screen retrieved via MCP. Divergence list documented.
  </verify>
  <acceptance_criteria>
    - mcp__figma__get_design_context called with nodeId="8554ef1a3efa42f9a07ad8774a690a7d"
    - Divergence list identifies background color, waveform color, and subtitle styling
  </acceptance_criteria>
  <done>Stitch Speaking Mode screen retrieved, all divergences from current implementation documented</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild SpeakingMode to match Stitch</name>
  <files>frontend/src/modes/SpeakingMode.tsx</files>
  <read_first>
    - frontend/src/modes/SpeakingMode.tsx (current state)
    - frontend/src/index.css (token values after Plan 01)
  </read_first>
  <action>
    Per D-05 (targeted rebuild) and Stitch divergences from Task 1:

    1. Replace `background: '#0a0a0a'` with `background: 'var(--color-background)'` (per Pitfall 6)
    2. Update WAVE_COLOR to match Stitch Speaking Mode screen:
       - If Stitch shows #ad89ff (secondary), use `var(--color-secondary)` or the hex directly
       - If Stitch shows #9b59b6 (current), keep as-is
       - If Stitch shows a different value, use that value
    3. Update atmospheric glow div to match Stitch:
       - Size, blur radius, opacity, gradient colors
       - Use ambient shadow tokens (primary-dim / secondary-dim) if appropriate per D-12
    4. Verify subtitle styling matches Stitch:
       - Font family, size, color, positioning, text shadow
       - Add top-left gradient to any glassmorphism surfaces per D-11 if Stitch shows them
    5. Ensure any glassmorphism elements include both `backdropFilter` and `WebkitBackdropFilter` (Pitfall 3)
    6. Verify all animation easing uses [0.22, 1, 0.36, 1] (D-07) — current subtitle animation is already correct
    7. Adjust any other divergences from Stitch

    **Preserve:** Canvas waveform rendering logic, useWaveVisualizer integration, onTap handler, subtitle text clipping logic, component props interface.
  </action>
  <verify>
    <automated>cd /Users/alikeforalike/Documents/Dev/jarvis-v1/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SpeakingMode.tsx contains `var(--color-background)` and does NOT contain `#0a0a0a`
    - SpeakingMode.tsx WAVE_COLOR matches Stitch output
    - SpeakingMode.tsx does NOT contain the string `easeInOut`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>SpeakingMode matches Stitch Speaking Mode screen — correct background, waveform color, atmospheric glow, and subtitle styling</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `grep -n "#0a0a0a" frontend/src/modes/SpeakingMode.tsx` returns zero matches
- `grep -n "easeInOut" frontend/src/modes/SpeakingMode.tsx` returns zero matches
</verification>

<success_criteria>
SpeakingMode matches its Stitch screen. Background uses design token. Waveform color verified against Stitch. All styling matches Stitch pixel-for-pixel.
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-audit-rebuild/04-03-SUMMARY.md`
</output>
