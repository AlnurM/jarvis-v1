---
phase: 04-design-audit-rebuild
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/modes/WeatherMode.tsx
  - frontend/src/modes/PrayerMode.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "WeatherMode and PrayerMode each match their respective Stitch screens"
    - "Glassmorphism cards include top-left gradient per D-11"
    - "No string easing ('easeInOut', 'ease') in animation objects — all use cubic-bezier array"
    - "Ambient shadows use primary-dim or secondary-dim tokens, not black"
  artifacts:
    - path: "frontend/src/modes/WeatherMode.tsx"
      provides: "Weather mode matching Stitch screen"
      contains: "rgba(255,255,255,0.05)"
    - path: "frontend/src/modes/PrayerMode.tsx"
      provides: "Prayer mode matching Stitch screen"
      contains: "rgba(255,255,255,0.05)"
  key_links:
    - from: "frontend/src/modes/WeatherMode.tsx"
      to: "frontend/src/index.css"
      via: "CSS custom property var() references"
      pattern: "var\\(--color-"
    - from: "frontend/src/modes/PrayerMode.tsx"
      to: "frontend/src/index.css"
      via: "CSS custom property var() references"
      pattern: "var\\(--color-"
---

<objective>
Pull Stitch screens for Weather Mode and Prayer Times Mode, then rebuild both components to match pixel-for-pixel.

Purpose: WeatherMode and PrayerMode are the two specialized information display modes. They already have working glassmorphism but are missing the top-left gradient (D-11) and use string easing in some animations.
Output: Both modes rebuilt to Stitch fidelity with correct glassmorphism, shadows, and easing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-audit-rebuild/04-CONTEXT.md
@.planning/phases/04-design-audit-rebuild/04-RESEARCH.md
@.planning/phases/04-design-audit-rebuild/04-01-SUMMARY.md
@design.md

<interfaces>
<!-- WeatherMode known gaps from RESEARCH.md audit: -->
- Hourly cards missing top-left gradient (D-11): need linear-gradient(to bottom right, rgba(255,255,255,0.05), rgba(255,255,255,0))
- Condition emoji animation uses ease: 'easeInOut' (line ~122) — needs [0.22, 1, 0.36, 1]
- Hourly card shadows: currently none — should use primary-dim at 4-6% opacity per D-12
- Background: uses getConditionBg() function with hardcoded hex values — verify against Stitch

<!-- PrayerMode known gaps from RESEARCH.md audit: -->
- Prayer row cards missing top-left gradient (D-11)
- CSS transition uses 'ease' string: `transition: 'opacity 0.3s ease, background 0.3s ease'` — needs custom easing
- Prayer row card shadows: "next" prayer has boxShadow but others have 'none' — verify against Stitch
- Background: '#0e0e0e' hardcoded (matches token but should use var(--color-background))

<!-- Glassmorphism pattern per D-11: -->
background: rgba(32, 31, 31, 0.4);
backdropFilter: blur(24px);
WebkitBackdropFilter: blur(24px);
/* PLUS the missing top-left gradient overlay: */
/* Apply as a pseudo-element or nested div, or use background shorthand with multiple layers */

<!-- Tokens available after Plan 01: -->
--color-primary-dim (for ambient shadows)
--color-secondary-dim (for ambient shadows)
--color-background (#0e0e0e)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pull Stitch screens for Weather Mode and Prayer Times Mode</name>
  <files>none (extraction only)</files>
  <read_first>
    - frontend/src/modes/WeatherMode.tsx (current implementation)
    - frontend/src/modes/PrayerMode.tsx (current implementation)
    - design.md (section 4 elevation, section 5 cards/lists)
  </read_first>
  <action>
    1. Load the figma-implement-design skill by invoking the Skill tool with `figma:figma-implement-design`
    2. Pull Weather Mode screen:
       - Call `mcp__figma__get_design_context` with fileKey="7359010342585899885" and nodeId="46d9c2600c1948658c68a31705074ca7"
       - Call `mcp__figma__get_screenshot` with same fileKey and nodeId
    3. Pull Prayer Times Mode screen:
       - Call `mcp__figma__get_design_context` with fileKey="7359010342585899885" and nodeId="b9c8cef5cb4b4a9db5931e80797efe16"
       - Call `mcp__figma__get_screenshot` with same fileKey and nodeId
    4. Compare Stitch output against current implementations and identify ALL divergences:

       **WeatherMode divergences to check:**
       - Background: does Stitch use condition-based backgrounds or a flat color?
       - Temperature display: font size, weight, color — is #e8e8e8 correct?
       - Condition emoji: size, animation
       - Condition label: font, size, spacing, color
       - Hourly cards: glassmorphism style, top-left gradient presence, card size, spacing, border radius
       - Hourly card content: icon size, time format, temp display
       - Any elements in Stitch missing from current code

       **PrayerMode divergences to check:**
       - Background: #0e0e0e or different?
       - "Next prayer" label: font, size, color, spacing
       - Prayer name display: font, size, weight, color
       - Countdown timer: font, size, color
       - Prayer list rows: glassmorphism style, highlighted state colors, passed state opacity
       - Row spacing, padding, border radius
       - Any elements in Stitch missing from current code

    5. Record all divergences for Tasks 2 and 3.
  </action>
  <verify>
    Both Stitch screens retrieved via MCP. Divergence lists documented for WeatherMode and PrayerMode.
  </verify>
  <acceptance_criteria>
    - mcp__figma__get_design_context called with nodeId="46d9c2600c1948658c68a31705074ca7" (Weather)
    - mcp__figma__get_design_context called with nodeId="b9c8cef5cb4b4a9db5931e80797efe16" (Prayer)
    - Divergence lists identify glassmorphism, gradient, and easing issues at minimum
  </acceptance_criteria>
  <done>Stitch screens for Weather and Prayer modes retrieved, all divergences documented</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild WeatherMode to match Stitch</name>
  <files>frontend/src/modes/WeatherMode.tsx</files>
  <read_first>
    - frontend/src/modes/WeatherMode.tsx (current state)
    - frontend/src/index.css (token values after Plan 01)
  </read_first>
  <action>
    Per D-05 (targeted rebuild) and Stitch divergences from Task 1:

    1. ADD top-left gradient to hourly forecast glassmorphism cards per D-11.
       Apply as a CSS background with multiple layers. The pattern is:
       ```
       background: linear-gradient(to bottom right, rgba(255,255,255,0.05), rgba(255,255,255,0)) padding-box, rgba(32, 31, 31, 0.4)
       ```
       Or use a wrapper/pseudo approach if the above doesn't work with backdrop-filter.

    2. ADD ambient shadow to hourly cards per D-12:
       `boxShadow: '0 0 30px rgba(133, 173, 255, 0.05)'` (primary-dim at 5% opacity)
       Never use black (#000) shadows.

    3. REPLACE `ease: 'easeInOut'` on condition emoji animation (line ~122) with `ease: [0.22, 1, 0.36, 1]` per D-07.

    4. VERIFY temperature display styling matches Stitch: font size, weight, color (#e8e8e8 or Stitch value).

    5. VERIFY condition background colors match Stitch. If Stitch uses different values than getConditionBg(), update them.

    6. VERIFY hourly card border radius — current is `rounded-2xl` (1rem). If Stitch shows `--radius-xl` (1.5rem), update.

    7. Adjust any other divergences found in Task 1.

    **Preserve:** Type definitions (HourlyItem, WeatherData), helper functions (getConditionBg, getConditionEmoji, iconToEmoji, formatHour), store integration, overall layout structure.
  </action>
  <verify>
    <automated>cd /Users/alikeforalike/Documents/Dev/jarvis-v1/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - WeatherMode.tsx hourly card styling contains `rgba(255,255,255,0.05)` (top-left gradient per D-11)
    - WeatherMode.tsx does NOT contain the string `easeInOut`
    - WeatherMode.tsx hourly cards contain a boxShadow property (ambient shadow per D-12)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>WeatherMode matches Stitch Weather Mode screen — glassmorphism cards have top-left gradient, ambient shadows, and correct easing</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild PrayerMode to match Stitch</name>
  <files>frontend/src/modes/PrayerMode.tsx</files>
  <read_first>
    - frontend/src/modes/PrayerMode.tsx (current state)
    - frontend/src/index.css (token values after Plan 01)
  </read_first>
  <action>
    Per D-05 (targeted rebuild) and Stitch divergences from Task 1:

    1. REPLACE `background: '#0e0e0e'` with `background: 'var(--color-background)'` for the token reference (even though value is same — consistency).

    2. ADD top-left gradient to prayer row glassmorphism cards per D-11:
       For non-highlighted rows: add `rgba(255,255,255,0.05)` gradient overlay
       For highlighted (next prayer) row: maintain the primary glow tint but add the gradient

    3. REPLACE CSS `transition: 'opacity 0.3s ease, background 0.3s ease'` with custom easing:
       `transition: 'opacity 0.3s cubic-bezier(0.22, 1, 0.36, 1), background 0.3s cubic-bezier(0.22, 1, 0.36, 1)'`
       Per D-07, no standard easing anywhere.

    4. ADD ambient shadow to prayer row cards per D-12:
       All rows (not just "next"): `boxShadow: '0 0 30px rgba(133, 173, 255, 0.04)'` (subtle)
       Next prayer row: keep existing stronger glow

    5. VERIFY prayer name and countdown styling matches Stitch: font sizes, weights, colors.

    6. VERIFY prayer row border radius — current `rounded-2xl` (1rem). If Stitch shows `--radius-xl` (1.5rem), update.

    7. Adjust any other divergences from Stitch.

    **Preserve:** Type definitions (PrayerData), helper functions (toMinutes, computeCountdown, formatCountdown, getPrayerStatus), PRAYER_ORDER, store integration, countdown interval logic.
  </action>
  <verify>
    <automated>cd /Users/alikeforalike/Documents/Dev/jarvis-v1/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PrayerMode.tsx contains `var(--color-background)` for background
    - PrayerMode.tsx prayer rows contain `rgba(255,255,255,0.05)` (top-left gradient per D-11)
    - PrayerMode.tsx does NOT contain the string `0.3s ease` (replaced with custom easing)
    - PrayerMode.tsx contains `cubic-bezier(0.22, 1, 0.36, 1)` or `0.22, 1, 0.36, 1`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>PrayerMode matches Stitch Prayer Times Mode screen — glassmorphism rows have gradient, ambient shadows, and custom easing</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `grep -n "easeInOut" frontend/src/modes/WeatherMode.tsx frontend/src/modes/PrayerMode.tsx` returns zero matches
- `grep -n "0.3s ease" frontend/src/modes/PrayerMode.tsx` returns zero matches (old CSS easing removed)
- `grep "rgba(255,255,255,0.05)" frontend/src/modes/WeatherMode.tsx` returns at least 1 match
- `grep "rgba(255,255,255,0.05)" frontend/src/modes/PrayerMode.tsx` returns at least 1 match
</verification>

<success_criteria>
WeatherMode and PrayerMode both match their Stitch screens. Glassmorphism cards have top-left gradients and ambient shadows. All easing uses the custom cubic-bezier. Build passes clean.
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-audit-rebuild/04-04-SUMMARY.md`
</output>
