---
phase: 04-design-audit-rebuild
plan: 05
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - frontend/src/components/ModeRouter.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "No string easing ('easeInOut', 'easeIn', 'easeOut', 'linear') exists anywhere in frontend/src/ mode or component files"
    - "No hardcoded #0a0a0a exists anywhere in frontend/src/ mode files"
    - "No 1px border properties exist in frontend/src/modes/ files (No-Line Rule D-09)"
    - "No pure white #FFFFFF text color exists in frontend/src/modes/ files (D-10)"
    - "Build passes with zero errors"
  artifacts:
    - path: "frontend/src/components/ModeRouter.tsx"
      provides: "ModeRouter with verified correct easing throughout"
      contains: "[0.22, 1, 0.36, 1]"
  key_links:
    - from: "frontend/src/components/ModeRouter.tsx"
      to: "all mode components"
      via: "AnimatePresence wrapping"
      pattern: "AnimatePresence"
---

<objective>
Final design compliance audit: verify ModeRouter easing, run codebase-wide grep checks for design rule violations, and checkpoint for visual verification on iPad.

Purpose: Catch any remaining violations that slipped through individual mode rebuilds. The phase is not done until every design rule is enforced across the entire frontend.
Output: Clean codebase with zero design rule violations, verified by automated checks and human visual inspection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-audit-rebuild/04-CONTEXT.md
@.planning/phases/04-design-audit-rebuild/04-RESEARCH.md
@.planning/phases/04-design-audit-rebuild/04-02-SUMMARY.md
@.planning/phases/04-design-audit-rebuild/04-03-SUMMARY.md
@.planning/phases/04-design-audit-rebuild/04-04-SUMMARY.md
@design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Codebase-wide design compliance audit and ModeRouter verification</name>
  <files>frontend/src/components/ModeRouter.tsx</files>
  <read_first>
    - frontend/src/components/ModeRouter.tsx (verify easing is still correct)
    - frontend/src/index.css (verify all tokens present)
  </read_first>
  <action>
    Run comprehensive grep checks across all frontend/src/ files and fix any violations found:

    **Check 1 — No string easing (D-07):**
    ```
    grep -rn "ease: 'easeInOut'\|ease: 'easeIn'\|ease: 'easeOut'\|ease: 'linear'" frontend/src/
    ```
    If ANY matches found: replace with `ease: [0.22, 1, 0.36, 1]`

    **Check 2 — No hardcoded #0a0a0a (Pitfall 6):**
    ```
    grep -rn "#0a0a0a" frontend/src/
    ```
    If ANY matches found: replace with `var(--color-background)`

    **Check 3 — No-Line Rule (D-09):**
    ```
    grep -rn "border:" frontend/src/modes/ frontend/src/components/
    ```
    If matches found that are NOT border-radius: remove the border property

    **Check 4 — No pure white text (D-10):**
    ```
    grep -rn "#FFFFFF\|#ffffff\|#FFF\|#fff" frontend/src/modes/
    ```
    If ANY matches found as text color: replace with `var(--color-on-surface-variant)` or `#e8e8e8`

    **Check 5 — Old token names removed (Pitfall 2):**
    ```
    grep -rn "surface-low[^e]" frontend/src/
    ```
    (matches surface-low but not surface-lowest)
    If ANY matches found: update to surface-container-low

    **Check 6 — ModeRouter easing still correct:**
    Read ModeRouter.tsx and verify:
    - modeVariants uses [0.22, 1, 0.36, 1] for both animate and exit transitions
    - The idle state OrbAnimation div uses var(--color-background)
    - "Tap to speak" label uses var(--color-on-surface-variant)

    If ModeRouter idle view background is `var(--color-background)` (already correct), no changes needed.
    If any issues found in ModeRouter, fix them.

    **Check 7 — Build verification:**
    ```
    cd frontend && npm run build
    ```
    Must exit with code 0.
  </action>
  <verify>
    <automated>cd /Users/alikeforalike/Documents/Dev/jarvis-v1 && grep -rn "easeInOut\|easeIn'\|easeOut'" frontend/src/modes/ frontend/src/components/ | grep -v node_modules | grep -v "\.d\.ts" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "easeInOut" frontend/src/modes/ frontend/src/components/` returns 0 matches
    - `grep -rn "#0a0a0a" frontend/src/modes/ frontend/src/components/` returns 0 matches
    - `grep -rn "#FFFFFF\|#ffffff" frontend/src/modes/` returns 0 matches
    - `npm run build` in frontend/ exits with code 0
    - ModeRouter.tsx contains `[0.22, 1, 0.36, 1]` (at least 2 occurrences)
  </acceptance_criteria>
  <done>Zero design rule violations in frontend/src/ — all easing, colors, backgrounds, and borders comply with design system</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification on iPad Safari</name>
  <files>none (visual verification only)</files>
  <action>
    Present the rebuilt UI for human visual verification. All 5 visual modes have been rebuilt to match their Stitch design screens.

    Verification steps:
    1. Deploy to Railway or run locally: `cd frontend && npm run dev`
    2. Open in iPad Safari (or desktop browser for initial check)
    3. Verify each mode visually against its Stitch screen:
       - Idle state: OrbAnimation breathing with correct colors (not oversaturated), "Tap to speak" in muted gray
       - Listening: Dark background (not pure black), blue waveform, faded "Listening..." label
       - Thinking: Breathing orb with blue-to-purple color shift, no text, ambient glow behind orb
       - Speaking: Purple waveform, subtitle text in muted gray (not white), atmospheric glow
       - Weather: Temperature display, condition icon, hourly cards with glassmorphism + subtle gradient sheen
       - Prayer: Next prayer name large, countdown in blue, prayer list with glassmorphism rows, highlighted next prayer
    4. Verify mode transitions use the "heavy glass" easing — not snappy standard easing
    5. Check that no harsh 1px borders appear anywhere
    6. Check that body text is never pure white — always the muted #adaaaa tone

    Resume signal: Type "approved" if all modes match Stitch screens, or describe specific issues to fix.
  </action>
  <verify>Human confirms visual fidelity by typing "approved" or describing issues</verify>
  <acceptance_criteria>
    - User has visually verified all 5 modes against Stitch screens
    - User has typed "approved" or provided feedback
  </acceptance_criteria>
  <done>User has approved visual fidelity of all modes against Stitch screens</done>
</task>

</tasks>

<verification>
- All automated grep checks pass with zero violations
- `npm run build` passes
- Human confirms visual fidelity against Stitch screens on iPad
</verification>

<success_criteria>
Zero design rule violations across the entire frontend. Every mode visually matches its Stitch screen. Human has confirmed visual fidelity.
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-audit-rebuild/04-05-SUMMARY.md`
</output>
