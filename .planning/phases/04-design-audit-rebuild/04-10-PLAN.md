---
phase: 04-design-audit-rebuild
plan: 10
type: execute
wave: 2
depends_on: ["04-06"]
files_modified:
  - frontend/src/modes/PrayerMode.tsx
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "PrayerMode shows header tabs: PRAYER TIMES (active) / QIBLA / MEDITATION"
    - "PrayerMode shows 'NEXT PRAYER' label above large prayer name"
    - "PrayerMode shows countdown digits with HOURS / MINUTES / SECONDS labels below"
    - "PrayerMode shows green location pill button with city + time"
    - "PrayerMode shows right-side date panel with weekday, Islamic date, and Golden Hour indicator"
    - "PrayerMode shows 5 prayer cards at bottom with time prominent and name below"
  artifacts:
    - path: "frontend/src/modes/PrayerMode.tsx"
      provides: "Full Stitch-fidelity prayer mode layout"
      contains: "NEXT PRAYER"
  key_links:
    - from: "frontend/src/modes/PrayerMode.tsx"
      to: "frontend/src/store/assistantStore.ts"
      via: "useAssistantStore for modeData"
      pattern: "useAssistantStore"
---

<objective>
Rebuild PrayerMode to match its Stitch screen: header tabs, NEXT PRAYER label, countdown with labeled segments, green location pill, right-side date panel, and restyled prayer cards. Fills AppShell content area.

Purpose: Close the PrayerMode layout gaps. Current implementation has prayer name + countdown + row list but is missing header tabs, countdown labels, location pill, date panel, and proper card styling.

Output: Rebuilt PrayerMode.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-design-audit-rebuild/04-CONTEXT.md
@.planning/phases/04-design-audit-rebuild/STITCH-SPECS.md
@.planning/phases/04-design-audit-rebuild/04-06-SUMMARY.md
@frontend/src/modes/PrayerMode.tsx
@frontend/src/index.css

<interfaces>
From frontend/src/modes/PrayerMode.tsx (current):
```typescript
interface PrayerData {
  Fajr: string    // "03:40" HH:MM
  Dhuhr: string
  Asr: string
  Maghrib: string
  Isha: string
}
const PRAYER_ORDER = ['Fajr', 'Dhuhr', 'Asr', 'Maghrib', 'Isha'] as const
type PrayerName = typeof PRAYER_ORDER[number]

// Helpers: toMinutes, computeCountdown, formatCountdown, getPrayerStatus
// computeCountdown returns { nextName: PrayerName; secondsLeft: number }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild PrayerMode with tabs, date panel, and restyled cards</name>
  <files>frontend/src/modes/PrayerMode.tsx</files>
  <read_first>
    - frontend/src/modes/PrayerMode.tsx (current implementation)
    - .planning/phases/04-design-audit-rebuild/STITCH-SPECS.md
    - frontend/src/index.css (tokens)
  </read_first>
  <action>
Rebuild PrayerMode.tsx to match the Stitch Prayer Times screen. Keep all helper functions (toMinutes, computeCountdown, formatCountdown, getPrayerStatus), PrayerData interface, PRAYER_ORDER, and the countdown useEffect. Restructure the JSX layout completely.

**New helper — split countdown into h/m/s segments:**
```typescript
function splitCountdown(seconds: number): { h: string; m: string; s: string } {
  const abs = Math.max(0, seconds)
  const h = Math.floor(abs / 3600)
  const m = Math.floor((abs % 3600) / 60)
  const s = abs % 60
  return {
    h: String(h).padStart(2, '0'),
    m: String(m).padStart(2, '0'),
    s: String(s).padStart(2, '0'),
  }
}
```

**Root container:**
```jsx
<motion.div
  className="w-full h-full flex flex-col overflow-hidden relative"
  style={{ background: 'var(--color-background)' }}
  initial={{ opacity: 0 }}
  animate={{ opacity: 1 }}
  transition={{ duration: 0.5, ease: [0.22, 1, 0.36, 1] }}
>
```

**Layout structure:**

1. **Top tab bar (centered, 48px):**
```jsx
<div className="flex items-center justify-center gap-8 py-3">
  {['PRAYER TIMES', 'QIBLA', 'MEDITATION'].map((tab) => (
    <span
      key={tab}
      style={{
        fontFamily: 'var(--font-label)',
        fontSize: '0.625rem',
        color: tab === 'PRAYER TIMES' ? 'var(--color-primary)' : 'var(--color-on-surface-variant)',
        letterSpacing: '0.15em',
        textTransform: 'uppercase',
        opacity: tab === 'PRAYER TIMES' ? 1 : 0.5,
        fontWeight: tab === 'PRAYER TIMES' ? 600 : 400,
      }}
    >
      {tab}
    </span>
  ))}
</div>
```

2. **Main content area — 2-column: center content + right date panel:**
```jsx
<div className="flex-1 flex overflow-hidden">
  {/* Left/Center: prayer name + countdown + location pill */}
  <div className="flex-1 flex flex-col items-center justify-center gap-4">
    {/* "NEXT PRAYER" label */}
    <p style={{
      fontFamily: 'var(--font-label)',
      fontSize: '0.625rem',
      color: 'var(--color-on-surface-variant)',
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      opacity: 0.7,
    }}>
      NEXT PRAYER
    </p>

    {/* Large prayer name */}
    <motion.h1
      key={countdown.nextName}
      initial={{ opacity: 0, y: 8 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.4, ease: [0.22, 1, 0.36, 1] }}
      style={{
        fontFamily: 'var(--font-label)',
        fontSize: 'clamp(3rem, 8vw, 5rem)',
        fontWeight: 700,
        color: '#e8e8e8',
        letterSpacing: '-0.02em',
        lineHeight: 1,
        textTransform: 'uppercase',
      }}
    >
      {countdown.nextName}
    </motion.h1>

    {/* Countdown with labeled segments */}
    {(() => {
      const { h, m, s } = splitCountdown(countdown.secondsLeft)
      return (
        <div className="flex items-start gap-6 mt-2">
          {/* Hours */}
          <div className="flex flex-col items-center">
            <span style={{
              fontFamily: 'var(--font-label)',
              fontSize: 'clamp(1.5rem, 4vw, 2.5rem)',
              fontWeight: 600,
              color: 'var(--color-primary)',
              fontVariantNumeric: 'tabular-nums',
              letterSpacing: '0.05em',
            }}>
              {h}
            </span>
            <span style={{
              fontFamily: 'var(--font-label)',
              fontSize: '0.5rem',
              color: 'var(--color-on-surface-variant)',
              letterSpacing: '0.15em',
              textTransform: 'uppercase',
              opacity: 0.6,
              marginTop: 4,
            }}>
              HOURS
            </span>
          </div>
          {/* Minutes */}
          <div className="flex flex-col items-center">
            <span style={{
              fontFamily: 'var(--font-label)',
              fontSize: 'clamp(1.5rem, 4vw, 2.5rem)',
              fontWeight: 600,
              color: 'var(--color-primary)',
              fontVariantNumeric: 'tabular-nums',
              letterSpacing: '0.05em',
            }}>
              {m}
            </span>
            <span style={{
              fontFamily: 'var(--font-label)',
              fontSize: '0.5rem',
              color: 'var(--color-on-surface-variant)',
              letterSpacing: '0.15em',
              textTransform: 'uppercase',
              opacity: 0.6,
              marginTop: 4,
            }}>
              MINUTES
            </span>
          </div>
          {/* Seconds */}
          <div className="flex flex-col items-center">
            <span style={{
              fontFamily: 'var(--font-label)',
              fontSize: 'clamp(1.5rem, 4vw, 2.5rem)',
              fontWeight: 600,
              color: 'var(--color-primary)',
              fontVariantNumeric: 'tabular-nums',
              letterSpacing: '0.05em',
            }}>
              {s}
            </span>
            <span style={{
              fontFamily: 'var(--font-label)',
              fontSize: '0.5rem',
              color: 'var(--color-on-surface-variant)',
              letterSpacing: '0.15em',
              textTransform: 'uppercase',
              opacity: 0.6,
              marginTop: 4,
            }}>
              SECONDS
            </span>
          </div>
        </div>
      )
    })()}

    {/* Green location pill button */}
    <div
      className="mt-4"
      style={{
        background: 'rgba(76, 175, 80, 0.15)',
        borderRadius: '999px',
        padding: '0.5rem 1.25rem',
        display: 'flex',
        alignItems: 'center',
        gap: 8,
      }}
    >
      <div style={{ width: 6, height: 6, borderRadius: '50%', background: '#4caf50' }} />
      <span style={{
        fontFamily: 'var(--font-label)',
        fontSize: '0.625rem',
        color: '#4caf50',
        letterSpacing: '0.1em',
        textTransform: 'uppercase',
      }}>
        ALMATY, KZ — {new Date().toLocaleTimeString('en', { hour: '2-digit', minute: '2-digit', timeZone: 'Asia/Almaty' })}
      </span>
    </div>
  </div>

  {/* Right date panel (200px wide) */}
  <div
    className="flex flex-col gap-4 justify-center px-6"
    style={{ width: 200, minWidth: 200 }}
  >
    {/* Weekday + date */}
    <div>
      <p style={{
        fontFamily: 'var(--font-display)',
        fontSize: '0.875rem',
        color: 'var(--color-on-surface)',
        fontWeight: 500,
      }}>
        {new Date().toLocaleDateString('en', { weekday: 'long', day: 'numeric', month: 'long', timeZone: 'Asia/Almaty' })}
      </p>
    </div>

    {/* Islamic date placeholder */}
    <div style={{
      background: 'linear-gradient(135deg, rgba(255,255,255,0.05) 0%, rgba(255,255,255,0) 100%)',
      backdropFilter: 'blur(24px)',
      WebkitBackdropFilter: 'blur(24px)',
      borderRadius: 'var(--radius-xl)',
      padding: '0.75rem 1rem',
    }}>
      <span style={{ fontFamily: 'var(--font-label)', fontSize: '0.5rem', color: 'var(--color-on-surface-variant)', letterSpacing: '0.1em', textTransform: 'uppercase', opacity: 0.6 }}>
        ISLAMIC DATE
      </span>
      <p style={{ fontFamily: 'var(--font-display)', fontSize: '0.75rem', color: 'var(--color-on-surface-variant)', marginTop: 4 }}>
        --
      </p>
    </div>

    {/* Golden Hour indicator */}
    <div style={{
      background: 'linear-gradient(135deg, rgba(255,255,255,0.05) 0%, rgba(255,255,255,0) 100%)',
      backdropFilter: 'blur(24px)',
      WebkitBackdropFilter: 'blur(24px)',
      borderRadius: 'var(--radius-xl)',
      padding: '0.75rem 1rem',
    }}>
      <span style={{ fontFamily: 'var(--font-label)', fontSize: '0.5rem', color: 'var(--color-on-surface-variant)', letterSpacing: '0.1em', textTransform: 'uppercase', opacity: 0.6 }}>
        GOLDEN HOUR
      </span>
      <p style={{ fontFamily: 'var(--font-label)', fontSize: '0.75rem', color: '#ffc107', marginTop: 4 }}>
        --
      </p>
    </div>
  </div>
</div>
```

3. **Bottom prayer cards — horizontal row of 5 cards:**
```jsx
<div className="px-8 pb-6 flex gap-3">
  {PRAYER_ORDER.map((name) => {
    const status = getPrayerStatus(name, data, countdown.nextName)
    const isNext = status === 'next'
    const isPast = status === 'past'

    return (
      <div
        key={name}
        className="flex-1 flex flex-col items-center py-4 px-3"
        style={{
          background: isNext
            ? 'linear-gradient(135deg, rgba(133, 173, 255, 0.12) 0%, rgba(173, 137, 255, 0.06) 100%), rgba(32, 31, 31, 0.4)'
            : 'linear-gradient(135deg, rgba(255,255,255,0.05) 0%, rgba(255,255,255,0) 100%), rgba(32, 31, 31, 0.4)',
          backdropFilter: 'blur(24px)',
          WebkitBackdropFilter: 'blur(24px)',
          boxShadow: isNext
            ? '0 0 30px rgba(133, 173, 255, 0.12), inset 0 0 0 1px rgba(133, 173, 255, 0.15)'
            : '0 0 30px rgba(133, 173, 255, 0.04)',
          borderRadius: 'var(--radius-xl)',
          opacity: isPast ? 0.35 : 1,
          transition: 'opacity 0.3s cubic-bezier(0.22, 1, 0.36, 1), background 0.3s cubic-bezier(0.22, 1, 0.36, 1)',
        }}
      >
        {/* Time prominent (large) */}
        <span style={{
          fontFamily: 'var(--font-label)',
          fontSize: '1.25rem',
          fontWeight: 600,
          color: isNext ? '#85adff' : 'var(--color-on-surface)',
          fontVariantNumeric: 'tabular-nums',
          letterSpacing: '0.02em',
        }}>
          {data[name]}
        </span>
        {/* Prayer name below (smaller) */}
        <span style={{
          fontFamily: 'var(--font-display)',
          fontSize: '0.75rem',
          color: isNext ? '#85adff' : 'var(--color-on-surface-variant)',
          marginTop: 6,
          fontWeight: isNext ? 600 : 400,
        }}>
          {name}
        </span>
        {/* Status dot */}
        {isNext && (
          <div style={{
            width: 6, height: 6, borderRadius: '50%',
            background: '#85adff',
            marginTop: 8,
            boxShadow: '0 0 8px rgba(133, 173, 255, 0.5)',
          }} />
        )}
      </div>
    )
  })}
</div>
```

Note on the "next" card highlight: Use `inset 0 0 0 1px rgba(133, 173, 255, 0.15)` as an inset box-shadow (NOT a border) to create the glow border effect. This respects the No-Line Rule (D-09) since it's a shadow, not a CSS border.

**Empty state** — keep existing but update root to `w-full h-full`.

**Design rules:**
- No CSS `border:` properties (D-09) — use inset box-shadow for the next-prayer glow
- No pure white #FFFFFF (D-10)
- Glassmorphism: blur(24px) + gradient (D-11)
- Easing: [0.22, 1, 0.36, 1] (D-07)
  </action>
  <verify>
    <automated>cd /Users/alikeforalike/Documents/Dev/jarvis-v1 && grep -c "NEXT PRAYER" frontend/src/modes/PrayerMode.tsx && grep -c "HOURS" frontend/src/modes/PrayerMode.tsx && grep -c "MINUTES" frontend/src/modes/PrayerMode.tsx && grep -c "SECONDS" frontend/src/modes/PrayerMode.tsx && grep -c "PRAYER TIMES" frontend/src/modes/PrayerMode.tsx && grep -c "GOLDEN HOUR" frontend/src/modes/PrayerMode.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Root container uses `w-full h-full` (NOT `w-screen h-screen`)
    - Contains tab bar with "PRAYER TIMES" (active, primary color), "QIBLA", "MEDITATION"
    - Contains "NEXT PRAYER" label above prayer name
    - Countdown split into three columns with HOURS/MINUTES/SECONDS labels below digits
    - Contains green location pill with "ALMATY, KZ" and current time
    - Contains right-side date panel (200px wide) with weekday, Islamic date placeholder, Golden Hour indicator
    - Bottom prayer cards: 5 cards in a horizontal row, time prominent (1.25rem), name below (0.75rem)
    - Next prayer card has blue glow (inset box-shadow), NOT a CSS border
    - splitCountdown helper function exists
    - No CSS `border:` properties (D-09)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>PrayerMode rebuilt with header tabs, NEXT PRAYER label, segmented countdown (HOURS/MINUTES/SECONDS), green location pill, right date panel (weekday + Islamic date + Golden Hour placeholders), restyled horizontal prayer cards with glow highlight. Fills AppShell content area. Build passes.</done>
</task>

</tasks>

<verification>
- PrayerMode shows tabs, countdown labels, location pill, date panel, restyled cards
- Root container is `w-full h-full` for AppShell compatibility
- `npm run build` succeeds
</verification>

<success_criteria>
PrayerMode matches Stitch screen: header tabs, NEXT PRAYER label, segmented countdown with labels, green location pill, right date panel, horizontal prayer cards with prominent time and glow on active card. Fills AppShell content area.
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-audit-rebuild/04-10-SUMMARY.md`
</output>
