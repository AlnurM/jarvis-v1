---
phase: 06-extended-modes
plan: 06
type: execute
wave: 4
depends_on: [06-04, 06-05]
files_modified:
  - backend/routers/chat.py
  - backend/requirements.txt
  - Dockerfile
autonomous: false
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04, SRCH-05, CAL-01, CAL-02, CAL-03, CAL-04, CAL-05, CAL-06, CAL-07, BRIEF-01, BRIEF-02, BRIEF-03, BRIEF-04, BRIEF-05]

user_setup:
  - service: brave_search
    why: "Search mode requires API key"
    env_vars:
      - name: BRAVE_SEARCH_API_KEY
        source: "Brave Search Dashboard -> API Keys"
  - service: google_calendar
    why: "Calendar mode requires OAuth credentials"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client"
      - name: GOOGLE_CLIENT_SECRET
        source: "Same OAuth 2.0 Client page"
      - name: GOOGLE_REDIRECT_URI
        source: "Set to https://<railway-domain>/api/auth/google/callback"
    dashboard_config:
      - task: "Create OAuth 2.0 Web Application credential"
        location: "Google Cloud Console -> APIs & Services -> Credentials"
      - task: "Enable Google Calendar API"
        location: "Google Cloud Console -> APIs & Services -> Library"
      - task: "Add redirect URI"
        location: "OAuth 2.0 Client -> Authorized redirect URIs"

must_haves:
  truths:
    - "Full test suite passes including all new search/calendar/auth/briefing tests"
    - "Dockerfile builds with Google packages"
    - "System prompt handles all 3 new fetch types correctly"
    - "All mode components render on device with correct glassmorphism and animations"
  artifacts:
    - path: "backend/routers/chat.py"
      provides: "Complete system prompt with all fetch triggers"
      contains: "fetch='search'"
    - path: "Dockerfile"
      provides: "Updated Docker build with Google packages"
      contains: "requirements.txt"
  key_links:
    - from: "backend/routers/chat.py"
      to: "All 3 fetch helpers"
      via: "SYSTEM_PROMPT instructs Claude to trigger correct fetch type"
      pattern: "fetch=.search.|fetch=.calendar.|fetch=.briefing."
---

<objective>
Final integration: verify full test suite, update Dockerfile if needed, and validate all three new modes on iPad device.

Purpose: Ensures all Phase 6 requirements work end-to-end. This is the integration checkpoint before Phase 7.
Output: All tests green, Dockerfile ready, device validation complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-extended-modes/06-CONTEXT.md
@.planning/phases/06-extended-modes/06-02-SUMMARY.md
@.planning/phases/06-extended-modes/06-03-SUMMARY.md
@.planning/phases/06-extended-modes/06-04-SUMMARY.md
@.planning/phases/06-extended-modes/06-05-SUMMARY.md

@backend/routers/chat.py
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite validation and Dockerfile update</name>
  <files>Dockerfile, backend/routers/chat.py</files>
  <read_first>Dockerfile, backend/requirements.txt, backend/routers/chat.py</read_first>
  <action>
    1. Run the full test suite and fix any failures:
       ```bash
       .venv/bin/python -m pytest tests/ -v
       ```
       If any test fails, diagnose and fix. Common issues:
       - Import errors from new functions
       - Missing mock patches for new dispatch branches
       - System prompt string concatenation errors

    2. Verify the Dockerfile's pip install step will pick up the new Google packages from requirements.txt. The existing Dockerfile should already do `pip install -r requirements.txt` — confirm this. If the Dockerfile has a hardcoded pip install list, add the Google packages.

    3. Verify SYSTEM_PROMPT in chat.py is complete and well-formed. It should mention trigger rules for ALL fetch types: weather, prayer, search, calendar, briefing. Run a quick syntax check:
       ```bash
       .venv/bin/python -c "from routers.chat import SYSTEM_PROMPT; print(len(SYSTEM_PROMPT)); print('search' in SYSTEM_PROMPT, 'calendar' in SYSTEM_PROMPT, 'briefing' in SYSTEM_PROMPT)"
       ```

    4. Verify all dispatch cases exist in chat():
       ```bash
       grep -n 'fetch_type ==' backend/routers/chat.py
       ```
       Expected: weather, prayer, search, calendar, briefing (5 cases)

    5. Build frontend to verify no build errors:
       ```bash
       cd frontend && npx vite build 2>&1 | tail -10
       ```
  </action>
  <verify>
    <automated>cd /Users/alikeforalike/Documents/Dev/jarvis-v1 && .venv/bin/python -m pytest tests/ -v && cd frontend && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .venv/bin/python -m pytest tests/ -v reports 0 failures
    - grep "requirements.txt" Dockerfile returns match
    - grep "fetch_type ==" backend/routers/chat.py returns 5 matches (weather, prayer, search, calendar, briefing)
    - SYSTEM_PROMPT contains "search", "calendar", and "briefing"
    - frontend vite build completes without errors
  </acceptance_criteria>
  <done>Full test suite green. Dockerfile includes Google packages via requirements.txt. System prompt covers all fetch types. Frontend builds clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Device validation — all three new modes on iPad</name>
  <files>N/A</files>
  <action>
    Deploy to Railway and validate all three new modes on iPad Safari standalone PWA. Requires user to set Railway env vars (BRAVE_SEARCH_API_KEY, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI) and authorize Google Calendar via /api/auth/google before testing.

    Test search mode by asking a factual question — verify 3 glassmorphism cards animate in.
    Test calendar mode by asking about schedule — verify week view + event cards.
    Test calendar event creation by saying "add dentist Thursday 3pm" — verify event in Google Calendar.
    Test briefing mode by saying "morning briefing" — verify split layout with weather, events, summary, quote.
    Test content mode behavior: FloatingMic visible, background listening active, "domoy" dismisses.
  </action>
  <verify>
    <automated>echo "Manual device verification checkpoint — see how-to-verify steps below"</automated>
  </verify>
  <acceptance_criteria>
    - Search: 3 glassmorphism cards animate from bottom with favicon, source, title, snippet
    - Calendar: week view in top half, event cards in bottom half, voice-to-event works
    - Briefing: split layout, AI summary, quote at bottom
    - All modes: FloatingMic active, background listening, dismiss with "domoy"
    - AppShell labels correct: WEB INTELLIGENCE, SCHEDULE MATRIX, MORNING PROTOCOL
  </acceptance_criteria>
  <done>All three new modes verified working on iPad Safari standalone PWA with correct glassmorphism, animations, and content mode behavior.</done>
</task>

</tasks>

<verification>
- Full test suite: `.venv/bin/python -m pytest tests/ -v` — 0 failures
- Frontend build: `npx vite build` — clean
- Device: all 3 modes render correctly on iPad Safari standalone PWA
</verification>

<success_criteria>
- All automated tests pass (search, calendar, auth, briefing + existing)
- Dockerfile builds correctly with Google packages
- System prompt handles all fetch triggers
- iPad device shows correct UI for search, calendar, and briefing modes
- Voice-to-event creation works end-to-end
- Content mode behavior (FloatingMic, background listening, dismiss) works
</success_criteria>

<output>
After completion, create `.planning/phases/06-extended-modes/06-06-SUMMARY.md`
</output>
